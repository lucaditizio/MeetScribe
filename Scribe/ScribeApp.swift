import SwiftUI
import SwiftData

@main
struct ScribeApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Scribe — Loading...")
                .preferredColorScheme(.dark)
        }
    }
}